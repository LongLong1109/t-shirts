import { backgroundImage, colors } from '@/themes/colors'
import { fontSize, fontWeight } from '@/themes/fontStyle'
import { spacing } from '@/themes/spacing'

const themeConfig = {
  extend: {
    backgroundImage,
    fontSize,
    fontWeight,
    colors,
    spacing,
  },
}

export default themeConfig
